'use strict'

const router = require('express').Router()
const {} = require('../controllers/spaces.controller')
const {verifyToken} = require('../controllers/users.controller')



module.exports = router